# Archive input scaffold

Set `BULK_RNA_ARCHIVE` and `BULK_RNA_GTF`, then replace the example archive
member paths and metadata in `samples.tsv`. ZIP and tar-family archives are
supported. Source archives are read-only; selected BAMs are materialized under
the project result cache.
