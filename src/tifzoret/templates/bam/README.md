# BAM project scaffold

Replace the example rows in `samples.tsv`, set `BULK_RNA_BAM_ROOT` and
`BULK_RNA_GTF` (or replace those values in `project.yaml`), update the contrast
and gene sets, then run `tifzoret validate project.yaml`.
